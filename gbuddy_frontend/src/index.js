import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';  // replace './App' with the actual path to your App.js file

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);